# Finite-free integer homology paper

Build the independent v1.2.2 application paper with:

```sh
latexmk -pdf -interaction=nonstopmode main.tex
```

The paper describes the explicit finite-free chain-complex layer, finite
normalized simplicial frontend, exact comparison with mathlib categorical
homology, and the certified bridge to normalized Moore complexes.
